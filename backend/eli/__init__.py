"""Eli — a heart-shaped AI companion that lives on your desktop.

Backend package: FastAPI server + four agents (main, vision, automation, memory)
+ speech (STT, TTS, wake word). Everything runs locally except the optional
Claude API call used for reasoning and screen understanding.
"""

__version__ = "0.4.2"
